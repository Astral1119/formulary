{
  "name": "@formulary/gsheets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "push": "node build.js && npx clasp push",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@formulary/core": "workspace:*"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.0"
  }
}
