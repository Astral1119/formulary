{
  "name": "@formulary/auth-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "build": "tsc --noEmit",
    "clean": "rm -rf dist .wrangler"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
